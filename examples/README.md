# Examples

Runnable scripts that exercise specific RoboSandbox features. Each file
is standalone and self-contained — read the top docstring for usage.

| Script | What it shows |
|---|---|
| `list_ycb.py` | Print the bundled YCB catalog (ids, masses, hull counts, file sizes). |
| `spawn_ycb_scene.py` | Spawn three YCB objects on the Franka, render one frame. |

TODO 5.1 asks for ≥6 examples — more coming (custom_robot, custom_task,
custom_skill, record_demo, headless_eval, llm_guided, procedural_scene).
Pull requests welcome.
